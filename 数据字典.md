# 数据字典文档

## 版本信息
- **版本号**: 1.0.0
- **创建日期**: 2026-02-06
- **数据库类型**: MySQL 8.0
- **字符集**: UTF8MB4

---

## 目录
1. [用户表 (t_user)](#1-用户表-t_user)
2. [商品表 (t_product)](#2-商品表-t_product)
3. [订单表 (t_order)](#3-订单表-t_order)
4. [订单明细表 (t_order_detail)](#4-订单明细表-t_order_detail)
5. [用户地址表 (t_user_address)](#5-用户地址表-t_user_address)

---

## 1. 用户表 (t_user)

### 表说明
存储系统用户的基本信息，包括登录凭证、个人资料等核心数据。

### 表结构

| 字段名 | 字段类型 | 长度 | 是否主键 | 是否为空 | 默认值 | 字段说明 |
|--------|----------|------|----------|----------|--------|----------|
| user_id | BIGINT | - | 是 | 否 | - | 用户ID，主键，自增 |
| username | VARCHAR | 50 | 否 | 否 | - | 用户名，唯一 |
| password | VARCHAR | 128 | 否 | 否 | - | 密码（加密存储） |
| email | VARCHAR | 100 | 否 | 是 | NULL | 邮箱地址 |
| phone | VARCHAR | 20 | 否 | 是 | NULL | 手机号码 |
| real_name | VARCHAR | 50 | 否 | 是 | NULL | 真实姓名 |
| gender | TINYINT | - | 否 | 是 | NULL | 性别（0-未知，1-男，2-女） |
| birth_date | DATE | - | 否 | 是 | NULL | 出生日期 |
| avatar_url | VARCHAR | 255 | 否 | 是 | NULL | 头像URL |
| status | TINYINT | - | 否 | 否 | 1 | 账号状态（0-禁用，1-正常，2-冻结） |
| register_time | DATETIME | - | 否 | 否 | CURRENT_TIMESTAMP | 注册时间 |
| last_login_time | DATETIME | - | 否 | 是 | NULL | 最后登录时间 |
| create_time | DATETIME | - | 否 | 否 | CURRENT_TIMESTAMP | 创建时间 |
| update_time | DATETIME | - | 否 | 否 | CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP | 更新时间 |

### 索引信息
- **主键索引**: `PK_user_id` (user_id)
- **唯一索引**: `UK_username` (username)
- **唯一索引**: `UK_email` (email)
- **普通索引**: `IDX_phone` (phone)
- **普通索引**: `IDX_status` (status)

### 备注
- 密码字段需使用BCrypt或类似加密算法存储
- 邮箱和手机号至少需要填写一项用于找回密码

---

## 2. 商品表 (t_product)

### 表说明
存储商品的基本信息，包括商品名称、价格、库存等核心属性。

### 表结构

| 字段名 | 字段类型 | 长度 | 是否主键 | 是否为空 | 默认值 | 字段说明 |
|--------|----------|------|----------|----------|--------|----------|
| product_id | BIGINT | - | 是 | 否 | - | 商品ID，主键，自增 |
| product_code | VARCHAR | 50 | 否 | 否 | - | 商品编码，唯一 |
| product_name | VARCHAR | 200 | 否 | 否 | - | 商品名称 |
| category_id | BIGINT | - | 否 | 否 | - | 分类ID |
| brand | VARCHAR | 100 | 否 | 是 | NULL | 品牌 |
| price | DECIMAL | 10,2 | 否 | 否 | 0.00 | 售价 |
| cost_price | DECIMAL | 10,2 | 否 | 是 | NULL | 成本价 |
| stock_quantity | INT | - | 否 | 否 | 0 | 库存数量 |
| unit | VARCHAR | 20 | 否 | 否 | 件 | 单位 |
| description | TEXT | - | 否 | 是 | NULL | 商品描述 |
| main_image_url | VARCHAR | 255 | 否 | 是 | NULL | 主图URL |
| weight | DECIMAL | 10,2 | 否 | 是 | NULL | 重量（kg） |
| status | TINYINT | - | 否 | 否 | 1 | 商品状态（0-下架，1-上架，2-缺货） |
| sales_count | INT | - | 否 | 否 | 0 | 销量 |
| view_count | INT | - | 否 | 否 | 0 | 浏览量 |
| create_time | DATETIME | - | 否 | 否 | CURRENT_TIMESTAMP | 创建时间 |
| update_time | DATETIME | - | 否 | 否 | CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP | 更新时间 |

### 索引信息
- **主键索引**: `PK_product_id` (product_id)
- **唯一索引**: `UK_product_code` (product_code)
- **普通索引**: `IDX_category_id` (category_id)
- **普通索引**: `IDX_status` (status)
- **普通索引**: `IDX_brand` (brand)
- **复合索引**: `IDX_price_status` (price, status)

### 备注
- 价格字段使用DECIMAL类型确保精度
- 库存数量需要与订单系统联动，防止超卖

---

## 3. 订单表 (t_order)

### 表说明
存储订单的主要信息，包括订单状态、金额、收货地址等。

### 表结构

| 字段名 | 字段类型 | 长度 | 是否主键 | 是否为空 | 默认值 | 字段说明 |
|--------|----------|------|----------|----------|--------|----------|
| order_id | BIGINT | - | 是 | 否 | - | 订单ID，主键，自增 |
| order_no | VARCHAR | 32 | 否 | 否 | - | 订单号，唯一 |
| user_id | BIGINT | - | 否 | 否 | - | 用户ID |
| total_amount | DECIMAL | 10,2 | 否 | 否 | 0.00 | 订单总金额 |
| discount_amount | DECIMAL | 10,2 | 否 | 否 | 0.00 | 优惠金额 |
| freight_amount | DECIMAL | 10,2 | 否 | 否 | 0.00 | 运费 |
| pay_amount | DECIMAL | 10,2 | 否 | 否 | 0.00 | 实付金额 |
| pay_type | TINYINT | - | 否 | 是 | NULL | 支付方式（1-支付宝，2-微信，3-银行卡） |
| order_status | TINYINT | - | 否 | 否 | 0 | 订单状态（0-待付款，1-已付款，2-已发货，3-已完成，4-已取消，5-退款中，6-已退款） |
| receiver_name | VARCHAR | 50 | 否 | 否 | - | 收货人姓名 |
| receiver_phone | VARCHAR | 20 | 否 | 否 | - | 收货人电话 |
| receiver_province | VARCHAR | 50 | 否 | 否 | - | 收货省份 |
| receiver_city | VARCHAR | 50 | 否 | 否 | - | 收货城市 |
| receiver_district | VARCHAR | 50 | 否 | 否 | - | 收货区县 |
| receiver_address | VARCHAR | 255 | 否 | 否 | - | 详细地址 |
| remark | VARCHAR | 500 | 否 | 是 | NULL | 订单备注 |
| pay_time | DATETIME | - | 否 | 是 | NULL | 支付时间 |
| delivery_time | DATETIME | - | 否 | 是 | NULL | 发货时间 |
| complete_time | DATETIME | - | 否 | 是 | NULL | 完成时间 |
| create_time | DATETIME | - | 否 | 否 | CURRENT_TIMESTAMP | 创建时间 |
| update_time | DATETIME | - | 否 | 否 | CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP | 更新时间 |

### 索引信息
- **主键索引**: `PK_order_id` (order_id)
- **唯一索引**: `UK_order_no` (order_no)
- **普通索引**: `IDX_user_id` (user_id)
- **普通索引**: `IDX_order_status` (order_status)
- **普通索引**: `IDX_create_time` (create_time)
- **复合索引**: `IDX_user_status` (user_id, order_status)

### 备注
- 订单号建议使用雪花算法或类似算法生成，保证唯一性
- 订单金额相关字段需要保持精度一致性
- 订单状态变更需要记录日志

---

## 4. 订单明细表 (t_order_detail)

### 表说明
存储订单中的商品明细信息，一个订单可以包含多个商品。

### 表结构

| 字段名 | 字段类型 | 长度 | 是否主键 | 是否为空 | 默认值 | 字段说明 |
|--------|----------|------|----------|----------|--------|----------|
| detail_id | BIGINT | - | 是 | 否 | - | 明细ID，主键，自增 |
| order_id | BIGINT | - | 否 | 否 | - | 订单ID |
| product_id | BIGINT | - | 否 | 否 | - | 商品ID |
| product_code | VARCHAR | 50 | 否 | 否 | - | 商品编码 |
| product_name | VARCHAR | 200 | 否 | 否 | - | 商品名称（冗余存储） |
| product_image | VARCHAR | 255 | 否 | 是 | NULL | 商品图片（冗余存储） |
| price | DECIMAL | 10,2 | 否 | 否 | 0.00 | 商品单价 |
| quantity | INT | - | 否 | 否 | 1 | 购买数量 |
| discount_amount | DECIMAL | 10,2 | 否 | 否 | 0.00 | 优惠金额 |
| subtotal_amount | DECIMAL | 10,2 | 否 | 否 | 0.00 | 小计金额 |
| create_time | DATETIME | - | 否 | 否 | CURRENT_TIMESTAMP | 创建时间 |
| update_time | DATETIME | - | 否 | 否 | CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP | 更新时间 |

### 索引信息
- **主键索引**: `PK_detail_id` (detail_id)
- **普通索引**: `IDX_order_id` (order_id)
- **普通索引**: `IDX_product_id` (product_id)

### 备注
- 冗余存储商品名称和图片，防止商品信息变更影响历史订单
- 小计金额 = 单价 × 数量 - 优惠金额

---

## 5. 用户地址表 (t_user_address)

### 表说明
存储用户的收货地址信息，支持多地址管理。

### 表结构

| 字段名 | 字段类型 | 长度 | 是否主键 | 是否为空 | 默认值 | 字段说明 |
|--------|----------|------|----------|----------|--------|----------|
| address_id | BIGINT | - | 是 | 否 | - | 地址ID，主键，自增 |
| user_id | BIGINT | - | 否 | 否 | - | 用户ID |
| receiver_name | VARCHAR | 50 | 否 | 否 | - | 收货人姓名 |
| receiver_phone | VARCHAR | 20 | 否 | 否 | - | 收货人电话 |
| province | VARCHAR | 50 | 否 | 否 | - | 省份 |
| city | VARCHAR | 50 | 否 | 否 | - | 城市 |
| district | VARCHAR | 50 | 否 | 否 | - | 区县 |
| detail_address | VARCHAR | 255 | 否 | 否 | - | 详细地址 |
| postal_code | VARCHAR | 10 | 否 | 是 | NULL | 邮政编码 |
| address_tag | VARCHAR | 20 | 否 | 是 | NULL | 地址标签（家、公司等） |
| is_default | TINYINT | - | 否 | 否 | 0 | 是否默认地址（0-否，1-是） |
| longitude | DECIMAL | 10,6 | 否 | 是 | NULL | 经度 |
| latitude | DECIMAL | 10,6 | 否 | 是 | NULL | 纬度 |
| create_time | DATETIME | - | 否 | 否 | CURRENT_TIMESTAMP | 创建时间 |
| update_time | DATETIME | - | 否 | 否 | CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP | 更新时间 |

### 索引信息
- **主键索引**: `PK_address_id` (address_id)
- **普通索引**: `IDX_user_id` (user_id)
- **复合索引**: `IDX_user_default` (user_id, is_default)

### 备注
- 每个用户只能有一个默认地址
- 设置新默认地址时需要将其他地址的is_default设置为0
- 经纬度字段可用于计算配送距离

---

## 表关系说明

### ER关系图

```
t_user (1) ----< (N) t_order
t_user (1) ----< (N) t_user_address
t_order (1) ----< (N) t_order_detail
t_product (1) ----< (N) t_order_detail
```

### 外键约束说明

- `t_order.user_id` → `t_user.user_id`
- `t_order_detail.order_id` → `t_order.order_id`
- `t_order_detail.product_id` → `t_product.product_id`
- `t_user_address.user_id` → `t_user.user_id`

---

## 数据完整性规则

1. **级联删除规则**
   - 删除用户时，需要先处理该用户的订单（建议软删除）
   - 删除订单时，同时删除订单明细
   
2. **数据验证规则**
   - 邮箱格式需符合标准邮箱格式
   - 手机号需符合中国大陆手机号格式
   - 金额字段不能为负数
   - 库存数量不能为负数

3. **业务规则**
   - 订单完成后不允许修改
   - 商品下架后不能创建新订单
   - 用户被禁用后不能下单

---

## 变更日志

| 版本 | 日期 | 修改人 | 修改内容 |
|------|------|--------|----------|
| 1.0.0 | 2026-02-06 | System | 初始版本，创建5张核心业务表 |

---

## 附录

### 数据库命名规范
- 表名：使用小写字母，单词间用下划线分隔，统一使用 `t_` 前缀
- 字段名：使用小写字母，单词间用下划线分隔
- 索引名：主键索引使用 `PK_` 前缀，唯一索引使用 `UK_` 前缀，普通索引使用 `IDX_` 前缀

### 数据类型选择建议
- 整数：根据数值范围选择 TINYINT、INT、BIGINT
- 金额：统一使用 DECIMAL(10,2)
- 字符串：定长使用 CHAR，变长使用 VARCHAR
- 时间：日期使用 DATE，日期时间使用 DATETIME
- 文本：短文本使用 VARCHAR，长文本使用 TEXT
