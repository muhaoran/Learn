# 数据字典

## 文档说明

本文档用于描述系统中所有数据表的结构定义、字段说明和约束关系。

**版本信息：** v1.0  
**更新日期：** 2026-02-05  
**维护人员：** 待定

---

## 1. 用户表 (users)

### 表说明
存储系统用户的基本信息

### 字段定义

| 字段名 | 数据类型 | 长度 | 是否主键 | 是否为空 | 默认值 | 说明 |
|--------|---------|------|---------|---------|--------|------|
| id | INT | - | 是 | 否 | AUTO_INCREMENT | 用户唯一标识 |
| username | VARCHAR | 50 | 否 | 否 | - | 用户名 |
| email | VARCHAR | 100 | 否 | 否 | - | 电子邮箱 |
| password | VARCHAR | 255 | 否 | 否 | - | 加密后的密码 |
| phone | VARCHAR | 20 | 否 | 是 | NULL | 手机号码 |
| status | TINYINT | - | 否 | 否 | 1 | 用户状态：0-禁用，1-正常 |
| created_at | DATETIME | - | 否 | 否 | CURRENT_TIMESTAMP | 创建时间 |
| updated_at | DATETIME | - | 否 | 否 | CURRENT_TIMESTAMP | 更新时间 |

### 索引定义

| 索引名 | 索引类型 | 字段 | 说明 |
|--------|---------|------|------|
| PRIMARY | 主键索引 | id | 主键 |
| idx_username | 唯一索引 | username | 用户名唯一 |
| idx_email | 唯一索引 | email | 邮箱唯一 |
| idx_phone | 普通索引 | phone | 手机号查询 |

---

## 2. 角色表 (roles)

### 表说明
定义系统中的角色信息

### 字段定义

| 字段名 | 数据类型 | 长度 | 是否主键 | 是否为空 | 默认值 | 说明 |
|--------|---------|------|---------|---------|--------|------|
| id | INT | - | 是 | 否 | AUTO_INCREMENT | 角色唯一标识 |
| role_name | VARCHAR | 50 | 否 | 否 | - | 角色名称 |
| role_code | VARCHAR | 30 | 否 | 否 | - | 角色编码 |
| description | VARCHAR | 255 | 否 | 是 | NULL | 角色描述 |
| status | TINYINT | - | 否 | 否 | 1 | 状态：0-禁用，1-正常 |
| created_at | DATETIME | - | 否 | 否 | CURRENT_TIMESTAMP | 创建时间 |
| updated_at | DATETIME | - | 否 | 否 | CURRENT_TIMESTAMP | 更新时间 |

### 索引定义

| 索引名 | 索引类型 | 字段 | 说明 |
|--------|---------|------|------|
| PRIMARY | 主键索引 | id | 主键 |
| idx_role_code | 唯一索引 | role_code | 角色编码唯一 |

---

## 3. 用户角色关联表 (user_roles)

### 表说明
用户与角色的多对多关联关系

### 字段定义

| 字段名 | 数据类型 | 长度 | 是否主键 | 是否为空 | 默认值 | 说明 |
|--------|---------|------|---------|---------|--------|------|
| id | INT | - | 是 | 否 | AUTO_INCREMENT | 唯一标识 |
| user_id | INT | - | 否 | 否 | - | 用户ID |
| role_id | INT | - | 否 | 否 | - | 角色ID |
| created_at | DATETIME | - | 否 | 否 | CURRENT_TIMESTAMP | 创建时间 |

### 索引定义

| 索引名 | 索引类型 | 字段 | 说明 |
|--------|---------|------|------|
| PRIMARY | 主键索引 | id | 主键 |
| idx_user_role | 唯一索引 | user_id, role_id | 用户角色关联唯一 |
| idx_role_id | 普通索引 | role_id | 角色ID查询 |

### 外键约束

| 约束名 | 外键字段 | 参照表 | 参照字段 | 删除规则 | 更新规则 |
|--------|---------|--------|---------|---------|---------|
| fk_user_roles_user | user_id | users | id | CASCADE | CASCADE |
| fk_user_roles_role | role_id | roles | id | CASCADE | CASCADE |

---

## 4. 权限表 (permissions)

### 表说明
定义系统中的权限点

### 字段定义

| 字段名 | 数据类型 | 长度 | 是否主键 | 是否为空 | 默认值 | 说明 |
|--------|---------|------|---------|---------|--------|------|
| id | INT | - | 是 | 否 | AUTO_INCREMENT | 权限唯一标识 |
| permission_name | VARCHAR | 50 | 否 | 否 | - | 权限名称 |
| permission_code | VARCHAR | 50 | 否 | 否 | - | 权限编码 |
| resource_type | VARCHAR | 30 | 否 | 否 | - | 资源类型：menu-菜单，button-按钮，api-接口 |
| resource_path | VARCHAR | 255 | 否 | 是 | NULL | 资源路径 |
| parent_id | INT | - | 否 | 是 | NULL | 父权限ID |
| sort_order | INT | - | 否 | 否 | 0 | 排序号 |
| description | VARCHAR | 255 | 否 | 是 | NULL | 权限描述 |
| status | TINYINT | - | 否 | 否 | 1 | 状态：0-禁用，1-正常 |
| created_at | DATETIME | - | 否 | 否 | CURRENT_TIMESTAMP | 创建时间 |
| updated_at | DATETIME | - | 否 | 否 | CURRENT_TIMESTAMP | 更新时间 |

### 索引定义

| 索引名 | 索引类型 | 字段 | 说明 |
|--------|---------|------|------|
| PRIMARY | 主键索引 | id | 主键 |
| idx_permission_code | 唯一索引 | permission_code | 权限编码唯一 |
| idx_parent_id | 普通索引 | parent_id | 父权限查询 |

---

## 5. 角色权限关联表 (role_permissions)

### 表说明
角色与权限的多对多关联关系

### 字段定义

| 字段名 | 数据类型 | 长度 | 是否主键 | 是否为空 | 默认值 | 说明 |
|--------|---------|------|---------|---------|--------|------|
| id | INT | - | 是 | 否 | AUTO_INCREMENT | 唯一标识 |
| role_id | INT | - | 否 | 否 | - | 角色ID |
| permission_id | INT | - | 否 | 否 | - | 权限ID |
| created_at | DATETIME | - | 否 | 否 | CURRENT_TIMESTAMP | 创建时间 |

### 索引定义

| 索引名 | 索引类型 | 字段 | 说明 |
|--------|---------|------|------|
| PRIMARY | 主键索引 | id | 主键 |
| idx_role_permission | 唯一索引 | role_id, permission_id | 角色权限关联唯一 |
| idx_permission_id | 普通索引 | permission_id | 权限ID查询 |

### 外键约束

| 约束名 | 外键字段 | 参照表 | 参照字段 | 删除规则 | 更新规则 |
|--------|---------|--------|---------|---------|---------|
| fk_role_permissions_role | role_id | roles | id | CASCADE | CASCADE |
| fk_role_permissions_permission | permission_id | permissions | id | CASCADE | CASCADE |

---

## 6. 日志表 (logs)

### 表说明
记录系统操作日志

### 字段定义

| 字段名 | 数据类型 | 长度 | 是否主键 | 是否为空 | 默认值 | 说明 |
|--------|---------|------|---------|---------|--------|------|
| id | BIGINT | - | 是 | 否 | AUTO_INCREMENT | 日志唯一标识 |
| user_id | INT | - | 否 | 是 | NULL | 操作用户ID |
| module | VARCHAR | 50 | 否 | 否 | - | 操作模块 |
| action | VARCHAR | 50 | 否 | 否 | - | 操作动作 |
| method | VARCHAR | 10 | 否 | 是 | NULL | 请求方法：GET/POST/PUT/DELETE |
| request_url | VARCHAR | 255 | 否 | 是 | NULL | 请求URL |
| request_params | TEXT | - | 否 | 是 | NULL | 请求参数 |
| ip_address | VARCHAR | 50 | 否 | 是 | NULL | IP地址 |
| user_agent | VARCHAR | 255 | 否 | 是 | NULL | 用户代理 |
| status | TINYINT | - | 否 | 否 | 1 | 执行状态：0-失败，1-成功 |
| error_msg | TEXT | - | 否 | 是 | NULL | 错误信息 |
| execution_time | INT | - | 否 | 是 | NULL | 执行时长(毫秒) |
| created_at | DATETIME | - | 否 | 否 | CURRENT_TIMESTAMP | 创建时间 |

### 索引定义

| 索引名 | 索引类型 | 字段 | 说明 |
|--------|---------|------|------|
| PRIMARY | 主键索引 | id | 主键 |
| idx_user_id | 普通索引 | user_id | 用户ID查询 |
| idx_module | 普通索引 | module | 模块查询 |
| idx_created_at | 普通索引 | created_at | 时间查询 |

---

## 附录

### 数据类型说明

| 数据类型 | 说明 | 示例 |
|---------|------|------|
| INT | 整数型，4字节 | 1, 100, -50 |
| BIGINT | 长整数型，8字节 | 9999999999 |
| TINYINT | 小整数型，1字节 | 0, 1 |
| VARCHAR | 可变长度字符串 | "hello" |
| TEXT | 长文本 | 大段文字内容 |
| DATETIME | 日期时间 | 2026-02-05 10:30:00 |

### 命名规范

1. **表名**：使用小写字母和下划线，复数形式（如 users, roles）
2. **字段名**：使用小写字母和下划线（如 user_id, created_at）
3. **索引名**：
   - 主键：PRIMARY
   - 唯一索引：idx_字段名
   - 普通索引：idx_字段名
   - 外键：fk_表名_字段名
4. **布尔值**：使用 TINYINT(1)，0表示false，1表示true

### 通用字段说明

- **id**: 所有表的主键，使用自增长整数
- **created_at**: 记录创建时间
- **updated_at**: 记录更新时间
- **status**: 记录状态，通常 0-禁用/删除，1-正常/激活

### 维护说明

1. 新增表时，需要在本文档中补充完整的表结构说明
2. 修改表结构时，需要同步更新本文档
3. 定期审查和维护数据字典，确保与实际数据库结构一致
4. 重大变更需要记录版本号和变更日期

---

**文档结束**
