# 数据字典文档

**项目名称：** 电商管理系统  
**文档版本：** v1.0  
**创建日期：** 2026-02-06  
**最后更新：** 2026-02-06

---

## 目录
1. [用户表（users）](#1-用户表users)
2. [产品表（products）](#2-产品表products)
3. [分类表（categories）](#3-分类表categories)
4. [订单表（orders）](#4-订单表orders)
5. [订单详情表（order_details）](#5-订单详情表order_details)

---

## 1. 用户表（users）

**表名：** `users`  
**说明：** 存储系统用户的基本信息

| 字段名 | 数据类型 | 长度 | 是否主键 | 是否为空 | 默认值 | 说明 |
|--------|----------|------|----------|----------|--------|------|
| user_id | INT | - | 是 | 否 | 自增 | 用户ID，主键 |
| username | VARCHAR | 50 | 否 | 否 | - | 用户名，唯一 |
| password | VARCHAR | 255 | 否 | 否 | - | 密码（加密存储） |
| email | VARCHAR | 100 | 否 | 否 | - | 电子邮箱，唯一 |
| phone | VARCHAR | 20 | 否 | 是 | NULL | 手机号码 |
| real_name | VARCHAR | 50 | 否 | 是 | NULL | 真实姓名 |
| gender | ENUM | - | 否 | 是 | NULL | 性别（male/female/other） |
| birth_date | DATE | - | 否 | 是 | NULL | 出生日期 |
| avatar_url | VARCHAR | 255 | 否 | 是 | NULL | 头像URL |
| status | TINYINT | - | 否 | 否 | 1 | 状态（0-禁用，1-正常） |
| created_at | TIMESTAMP | - | 否 | 否 | CURRENT_TIMESTAMP | 创建时间 |
| updated_at | TIMESTAMP | - | 否 | 否 | CURRENT_TIMESTAMP | 更新时间 |

**索引：**
- PRIMARY KEY: `user_id`
- UNIQUE KEY: `username`
- UNIQUE KEY: `email`
- INDEX: `idx_phone` (phone)
- INDEX: `idx_status` (status)

---

## 2. 产品表（products）

**表名：** `products`  
**说明：** 存储商品的详细信息

| 字段名 | 数据类型 | 长度 | 是否主键 | 是否为空 | 默认值 | 说明 |
|--------|----------|------|----------|----------|--------|------|
| product_id | INT | - | 是 | 否 | 自增 | 产品ID，主键 |
| category_id | INT | - | 否 | 否 | - | 分类ID，外键关联categories表 |
| product_name | VARCHAR | 200 | 否 | 否 | - | 产品名称 |
| product_code | VARCHAR | 50 | 否 | 否 | - | 产品编码，唯一 |
| description | TEXT | - | 否 | 是 | NULL | 产品描述 |
| price | DECIMAL | 10,2 | 否 | 否 | 0.00 | 价格 |
| cost_price | DECIMAL | 10,2 | 否 | 是 | NULL | 成本价 |
| stock_quantity | INT | - | 否 | 否 | 0 | 库存数量 |
| unit | VARCHAR | 20 | 否 | 是 | NULL | 计量单位 |
| image_url | VARCHAR | 255 | 否 | 是 | NULL | 主图片URL |
| status | TINYINT | - | 否 | 否 | 1 | 状态（0-下架，1-上架） |
| is_featured | TINYINT | - | 否 | 否 | 0 | 是否精选（0-否，1-是） |
| view_count | INT | - | 否 | 否 | 0 | 浏览次数 |
| sales_count | INT | - | 否 | 否 | 0 | 销售数量 |
| created_at | TIMESTAMP | - | 否 | 否 | CURRENT_TIMESTAMP | 创建时间 |
| updated_at | TIMESTAMP | - | 否 | 否 | CURRENT_TIMESTAMP | 更新时间 |

**索引：**
- PRIMARY KEY: `product_id`
- UNIQUE KEY: `product_code`
- INDEX: `idx_category` (category_id)
- INDEX: `idx_status` (status)
- INDEX: `idx_price` (price)
- INDEX: `idx_sales` (sales_count)

**外键约束：**
- FOREIGN KEY (category_id) REFERENCES categories(category_id)

---

## 3. 分类表（categories）

**表名：** `categories`  
**说明：** 存储产品分类信息，支持多级分类

| 字段名 | 数据类型 | 长度 | 是否主键 | 是否为空 | 默认值 | 说明 |
|--------|----------|------|----------|----------|--------|------|
| category_id | INT | - | 是 | 否 | 自增 | 分类ID，主键 |
| parent_id | INT | - | 否 | 是 | NULL | 父分类ID，NULL表示顶级分类 |
| category_name | VARCHAR | 100 | 否 | 否 | - | 分类名称 |
| category_code | VARCHAR | 50 | 否 | 否 | - | 分类编码，唯一 |
| description | TEXT | - | 否 | 是 | NULL | 分类描述 |
| icon_url | VARCHAR | 255 | 否 | 是 | NULL | 分类图标URL |
| sort_order | INT | - | 否 | 否 | 0 | 排序顺序 |
| level | TINYINT | - | 否 | 否 | 1 | 分类层级 |
| status | TINYINT | - | 否 | 否 | 1 | 状态（0-禁用，1-启用） |
| created_at | TIMESTAMP | - | 否 | 否 | CURRENT_TIMESTAMP | 创建时间 |
| updated_at | TIMESTAMP | - | 否 | 否 | CURRENT_TIMESTAMP | 更新时间 |

**索引：**
- PRIMARY KEY: `category_id`
- UNIQUE KEY: `category_code`
- INDEX: `idx_parent` (parent_id)
- INDEX: `idx_sort` (sort_order)
- INDEX: `idx_level` (level)

**外键约束：**
- FOREIGN KEY (parent_id) REFERENCES categories(category_id)

---

## 4. 订单表（orders）

**表名：** `orders`  
**说明：** 存储订单主表信息

| 字段名 | 数据类型 | 长度 | 是否主键 | 是否为空 | 默认值 | 说明 |
|--------|----------|------|----------|----------|--------|------|
| order_id | INT | - | 是 | 否 | 自增 | 订单ID，主键 |
| order_no | VARCHAR | 50 | 否 | 否 | - | 订单号，唯一 |
| user_id | INT | - | 否 | 否 | - | 用户ID，外键关联users表 |
| total_amount | DECIMAL | 10,2 | 否 | 否 | 0.00 | 订单总金额 |
| discount_amount | DECIMAL | 10,2 | 否 | 否 | 0.00 | 优惠金额 |
| shipping_fee | DECIMAL | 10,2 | 否 | 否 | 0.00 | 运费 |
| payment_amount | DECIMAL | 10,2 | 否 | 否 | 0.00 | 实付金额 |
| payment_method | VARCHAR | 50 | 否 | 是 | NULL | 支付方式 |
| payment_status | TINYINT | - | 否 | 否 | 0 | 支付状态（0-未支付，1-已支付，2-已退款） |
| payment_time | TIMESTAMP | - | 否 | 是 | NULL | 支付时间 |
| order_status | TINYINT | - | 否 | 否 | 0 | 订单状态（0-待支付，1-待发货，2-已发货，3-已完成，4-已取消） |
| shipping_name | VARCHAR | 50 | 否 | 是 | NULL | 收货人姓名 |
| shipping_phone | VARCHAR | 20 | 否 | 是 | NULL | 收货人电话 |
| shipping_address | VARCHAR | 255 | 否 | 是 | NULL | 收货地址 |
| shipping_no | VARCHAR | 100 | 否 | 是 | NULL | 物流单号 |
| shipping_company | VARCHAR | 50 | 否 | 是 | NULL | 物流公司 |
| remark | TEXT | - | 否 | 是 | NULL | 订单备注 |
| created_at | TIMESTAMP | - | 否 | 否 | CURRENT_TIMESTAMP | 创建时间 |
| updated_at | TIMESTAMP | - | 否 | 否 | CURRENT_TIMESTAMP | 更新时间 |

**索引：**
- PRIMARY KEY: `order_id`
- UNIQUE KEY: `order_no`
- INDEX: `idx_user` (user_id)
- INDEX: `idx_status` (order_status)
- INDEX: `idx_payment_status` (payment_status)
- INDEX: `idx_created` (created_at)

**外键约束：**
- FOREIGN KEY (user_id) REFERENCES users(user_id)

---

## 5. 订单详情表（order_details）

**表名：** `order_details`  
**说明：** 存储订单明细信息，记录每个订单包含的商品

| 字段名 | 数据类型 | 长度 | 是否主键 | 是否为空 | 默认值 | 说明 |
|--------|----------|------|----------|----------|--------|------|
| detail_id | INT | - | 是 | 否 | 自增 | 订单详情ID，主键 |
| order_id | INT | - | 否 | 否 | - | 订单ID，外键关联orders表 |
| product_id | INT | - | 否 | 否 | - | 产品ID，外键关联products表 |
| product_name | VARCHAR | 200 | 否 | 否 | - | 产品名称（冗余字段） |
| product_code | VARCHAR | 50 | 否 | 否 | - | 产品编码（冗余字段） |
| price | DECIMAL | 10,2 | 否 | 否 | 0.00 | 单价 |
| quantity | INT | - | 否 | 否 | 1 | 购买数量 |
| discount_amount | DECIMAL | 10,2 | 否 | 否 | 0.00 | 优惠金额 |
| subtotal | DECIMAL | 10,2 | 否 | 否 | 0.00 | 小计金额 |
| remark | VARCHAR | 255 | 否 | 是 | NULL | 备注 |
| created_at | TIMESTAMP | - | 否 | 否 | CURRENT_TIMESTAMP | 创建时间 |

**索引：**
- PRIMARY KEY: `detail_id`
- INDEX: `idx_order` (order_id)
- INDEX: `idx_product` (product_id)

**外键约束：**
- FOREIGN KEY (order_id) REFERENCES orders(order_id)
- FOREIGN KEY (product_id) REFERENCES products(product_id)

---

## 表关系说明

### 实体关系图（ER）

```
users (1) ----< (N) orders
          |
          |
categories (1) ----< (N) products
                         |
                         |
orders (1) ----< (N) order_details >---- (N) products

categories (1) ----< (N) categories (自关联)
```

### 关系描述

1. **用户与订单**：一个用户可以有多个订单（一对多）
2. **分类与产品**：一个分类可以包含多个产品（一对多）
3. **订单与订单详情**：一个订单包含多个订单详情（一对多）
4. **产品与订单详情**：一个产品可以出现在多个订单详情中（一对多）
5. **分类自关联**：分类表支持多级分类结构（自关联）

---

## 数据库设计说明

### 命名规范
- 表名使用小写字母和下划线
- 字段名使用小写字母和下划线
- 主键统一命名为 `表名_id`
- 时间字段统一使用 `created_at` 和 `updated_at`

### 数据类型规范
- 主键统一使用 `INT` 自增
- 金额统一使用 `DECIMAL(10,2)`
- 状态字段统一使用 `TINYINT`
- 时间字段统一使用 `TIMESTAMP`
- 长文本使用 `TEXT`，短文本使用 `VARCHAR`

### 性能优化建议
1. 为高频查询字段创建索引
2. 对外键字段创建索引
3. 订单详情表保留产品名称等冗余字段，避免频繁关联查询
4. 使用分区表存储历史订单数据
5. 定期归档历史数据

### 安全性建议
1. 密码字段必须加密存储（建议使用 bcrypt 或 Argon2）
2. 敏感数据字段考虑加密存储
3. 设置合适的权限控制
4. 定期备份数据库

---

**文档结束**
