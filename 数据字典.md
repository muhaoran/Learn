# 数据字典

## 文档说明

本文档记录系统中所有数据库表的结构、字段说明及相关约束信息。

**版本**: 1.0.0  
**更新日期**: 2026-02-05  
**维护人**: 开发团队

---

## 1. 用户表 (user)

**表说明**: 存储系统用户基本信息

| 字段名 | 数据类型 | 长度 | 是否必填 | 主键 | 默认值 | 说明 |
|--------|---------|------|---------|------|--------|------|
| id | BIGINT | - | 是 | 是 | 自增 | 用户唯一标识 |
| username | VARCHAR | 50 | 是 | 否 | - | 用户名，唯一 |
| password | VARCHAR | 255 | 是 | 否 | - | 密码（加密存储） |
| email | VARCHAR | 100 | 是 | 否 | - | 邮箱地址 |
| phone | VARCHAR | 20 | 否 | 否 | NULL | 手机号码 |
| nickname | VARCHAR | 50 | 否 | 否 | NULL | 用户昵称 |
| avatar | VARCHAR | 255 | 否 | 否 | NULL | 头像URL |
| status | TINYINT | - | 是 | 否 | 1 | 状态：0-禁用，1-启用 |
| create_time | DATETIME | - | 是 | 否 | CURRENT_TIMESTAMP | 创建时间 |
| update_time | DATETIME | - | 是 | 否 | CURRENT_TIMESTAMP | 更新时间 |

**索引**:
- `idx_username`: username (唯一索引)
- `idx_email`: email (唯一索引)
- `idx_status`: status (普通索引)

---

## 2. 角色表 (role)

**表说明**: 存储系统角色信息

| 字段名 | 数据类型 | 长度 | 是否必填 | 主键 | 默认值 | 说明 |
|--------|---------|------|---------|------|--------|------|
| id | BIGINT | - | 是 | 是 | 自增 | 角色唯一标识 |
| role_name | VARCHAR | 50 | 是 | 否 | - | 角色名称 |
| role_code | VARCHAR | 50 | 是 | 否 | - | 角色编码 |
| description | VARCHAR | 200 | 否 | 否 | NULL | 角色描述 |
| status | TINYINT | - | 是 | 否 | 1 | 状态：0-禁用，1-启用 |
| create_time | DATETIME | - | 是 | 否 | CURRENT_TIMESTAMP | 创建时间 |
| update_time | DATETIME | - | 是 | 否 | CURRENT_TIMESTAMP | 更新时间 |

**索引**:
- `idx_role_code`: role_code (唯一索引)

---

## 3. 用户角色关联表 (user_role)

**表说明**: 用户与角色的多对多关联关系

| 字段名 | 数据类型 | 长度 | 是否必填 | 主键 | 默认值 | 说明 |
|--------|---------|------|---------|------|--------|------|
| id | BIGINT | - | 是 | 是 | 自增 | 主键ID |
| user_id | BIGINT | - | 是 | 否 | - | 用户ID |
| role_id | BIGINT | - | 是 | 否 | - | 角色ID |
| create_time | DATETIME | - | 是 | 否 | CURRENT_TIMESTAMP | 创建时间 |

**索引**:
- `idx_user_id`: user_id (普通索引)
- `idx_role_id`: role_id (普通索引)
- `uk_user_role`: user_id, role_id (唯一索引)

**外键**:
- `fk_user_role_user`: user_id -> user(id)
- `fk_user_role_role`: role_id -> role(id)

---

## 4. 权限表 (permission)

**表说明**: 存储系统权限信息

| 字段名 | 数据类型 | 长度 | 是否必填 | 主键 | 默认值 | 说明 |
|--------|---------|------|---------|------|--------|------|
| id | BIGINT | - | 是 | 是 | 自增 | 权限唯一标识 |
| permission_name | VARCHAR | 50 | 是 | 否 | - | 权限名称 |
| permission_code | VARCHAR | 100 | 是 | 否 | - | 权限编码 |
| permission_type | VARCHAR | 20 | 是 | 否 | - | 权限类型：menu-菜单，button-按钮，api-接口 |
| parent_id | BIGINT | - | 否 | 否 | 0 | 父权限ID，0表示顶级权限 |
| path | VARCHAR | 200 | 否 | 否 | NULL | 菜单路径或API路径 |
| icon | VARCHAR | 50 | 否 | 否 | NULL | 图标 |
| sort | INT | - | 是 | 否 | 0 | 排序号 |
| status | TINYINT | - | 是 | 否 | 1 | 状态：0-禁用，1-启用 |
| create_time | DATETIME | - | 是 | 否 | CURRENT_TIMESTAMP | 创建时间 |
| update_time | DATETIME | - | 是 | 否 | CURRENT_TIMESTAMP | 更新时间 |

**索引**:
- `idx_permission_code`: permission_code (唯一索引)
- `idx_parent_id`: parent_id (普通索引)

---

## 5. 角色权限关联表 (role_permission)

**表说明**: 角色与权限的多对多关联关系

| 字段名 | 数据类型 | 长度 | 是否必填 | 主键 | 默认值 | 说明 |
|--------|---------|------|---------|------|--------|------|
| id | BIGINT | - | 是 | 是 | 自增 | 主键ID |
| role_id | BIGINT | - | 是 | 否 | - | 角色ID |
| permission_id | BIGINT | - | 是 | 否 | - | 权限ID |
| create_time | DATETIME | - | 是 | 否 | CURRENT_TIMESTAMP | 创建时间 |

**索引**:
- `idx_role_id`: role_id (普通索引)
- `idx_permission_id`: permission_id (普通索引)
- `uk_role_permission`: role_id, permission_id (唯一索引)

**外键**:
- `fk_role_permission_role`: role_id -> role(id)
- `fk_role_permission_permission`: permission_id -> permission(id)

---

## 数据类型说明

### 常用数据类型

| 类型 | 说明 | 适用场景 |
|------|------|---------|
| BIGINT | 大整数，8字节 | 主键ID、大数值 |
| INT | 整数，4字节 | 一般整数、计数 |
| TINYINT | 小整数，1字节 | 状态、标志位 |
| VARCHAR(n) | 可变长度字符串 | 文本内容 |
| CHAR(n) | 固定长度字符串 | 固定长度编码 |
| TEXT | 长文本 | 文章、描述等 |
| DATETIME | 日期时间 | 时间记录 |
| TIMESTAMP | 时间戳 | 自动更新时间 |
| DECIMAL(m,n) | 精确小数 | 金额、价格 |
| BOOLEAN | 布尔值 | 是/否判断 |

---

## 字段命名规范

### 通用规范

1. **字段名**: 小写字母，单词间用下划线分隔（snake_case）
2. **主键**: 统一命名为 `id`
3. **外键**: 格式为 `[关联表名]_id`
4. **时间字段**: 创建时间为 `create_time`，更新时间为 `update_time`
5. **状态字段**: 统一命名为 `status`
6. **删除标记**: 统一命名为 `is_deleted`

### 常用字段后缀

- `_id`: 标识符
- `_name`: 名称
- `_code`: 编码
- `_type`: 类型
- `_time`: 时间
- `_date`: 日期
- `_status`: 状态
- `_count`: 计数
- `_amount`: 金额
- `_url`: 链接地址

---

## 状态码说明

### 用户状态 (user.status)

| 值 | 说明 |
|----|------|
| 0 | 禁用 |
| 1 | 启用 |
| 2 | 已删除 |

### 权限类型 (permission.permission_type)

| 值 | 说明 |
|----|------|
| menu | 菜单权限 |
| button | 按钮权限 |
| api | 接口权限 |

---

## 关系图

```
user (用户表)
  ↓ 1:N
user_role (用户角色关联表)
  ↓ N:1
role (角色表)
  ↓ 1:N
role_permission (角色权限关联表)
  ↓ N:1
permission (权限表)
```

---

## 变更记录

| 版本 | 日期 | 变更内容 | 变更人 |
|------|------|---------|--------|
| 1.0.0 | 2026-02-05 | 初始版本创建 | 开发团队 |

---

## 附录

### 数据库设计原则

1. **三范式原则**: 确保数据库设计符合至少第三范式
2. **命名规范**: 遵循统一的命名规范
3. **索引优化**: 为常用查询字段添加合适的索引
4. **字段长度**: 根据实际需求合理设置字段长度
5. **默认值**: 为非必填字段设置合理的默认值
6. **时间戳**: 记录数据的创建和更新时间
7. **软删除**: 重要数据建议使用软删除（is_deleted标记）

### 维护说明

- 每次表结构变更时，需同步更新本文档
- 添加新表时，需补充完整的字段说明和索引信息
- 重大变更需在变更记录中记录
