# 使用指南

> 5 分钟了解如何使用 ChatBI

---

## 什么是 ChatBI

用自然语言描述数据需求，AI 自动生成符合业务口径的 Trino SQL。

```
你：查询最近 7 天的 DAU，按渠道分
AI：[生成 SQL + 技术方案说明]
```

---

## 如何提需求

一个好的需求描述包含四个要素：

| 要素 | 说明 | 示例 |
|------|------|------|
| **指标** | 查什么 | DAU、次日留存率、发帖数 |
| **时间** | 什么时间段 | 昨天、最近 7 天、1 月份 |
| **维度** | 如何分组 | 按渠道、按平台、按日期 |
| **筛选** | 有无特殊条件 | 新用户、iOS 平台 |

**好的需求**：
- "查询最近 7 天的 DAU，按日期和注册渠道分组"
- "计算 1 月份新注册用户的次日留存率"
- "统计上周各平台的发帖用户数"

**模糊的需求也没关系**，AI 会主动澄清：
- "看下用户情况" → AI 会问：具体看什么指标？时间范围？
- "统计留存" → AI 会问：哪种留存？哪个时间段的用户？

---

## AI 的处理步骤

1. **理解需求**：识别业务术语（包括黑话），提取指标、维度、时间、筛选
2. **主动澄清**：遇到模糊点时，提供选项让你选择，而不是开放式提问
3. **生成 SQL**：基于知识库生成 Trino SQL，符合业务定义
4. **输出说明**：技术方案、使用的表、字段含义、注意事项

---

## 你会得到什么

每次输出包含：
- 格式化的 SQL（带注释，可直接执行）
- 使用的表和计算逻辑说明
- 结果字段含义
- 性能和数据质量提示

---

## 示例对话

### 简单需求（直接生成）

**你**：查询昨天的 DAU

**AI**：
```
需求理解：查询昨天的日活跃用户数（DAU）。

SQL 语句：
SELECT
    date,
    COUNT(DISTINCT user_id) AS dau
FROM dws_user_daily
WHERE date = date_add('day', -1, current_date)
  AND is_active = 1
GROUP BY date

技术方案：
- 使用 dws_user_daily 汇总表，已预计算活跃标记，性能好
- 预计耗时：< 1 秒

注意事项：
- 数据为 T+1 更新，确保当天数据已就绪
```

### 模糊需求（先澄清）

**你**：帮我看下新用户的留存情况

**AI**：
```
您想查询新用户的留存情况，需要确认几点：

1. 时间范围（想看哪个时间段注册的新用户？）
   A. 最近 30 天  B. 本月  C. 上月  D. 其他

2. 留存类型
   A. 次日留存（D1）  B. 7 日留存（D7）  C. 留存曲线（D1/D7/D30）
   推荐：A

3. 是否分维度？
   A. 不需要，只看整体  B. 按注册渠道  C. 按日期（看每天的留存变化）
   推荐：C
```

**你**：A, A, C

**AI**：[生成最近 30 天新用户 D1 留存率、按注册日期分组的 SQL]

---

## 遇到问题

| 情况 | 处理方式 |
|------|----------|
| SQL 不准确 | 告诉 AI 哪里不对，AI 重新生成 |
| AI 理解有偏差 | 纠正理解，或重新描述需求 |
| 知识库中没有该指标 | AI 会说明缺少什么，你临时提供定义后继续 |
| 需求超出数据能力 | AI 会说明原因并提供替代方案 |

---

## 反馈

发现 SQL 不准确或有改进建议，直接告诉 AI：
- AI 会记录到 `feedback/` 目录
- 建议同时将正确的定义补充到 `knowledge/` 对应位置

---

更多信息：
- 系统架构：`README.md`
- 知识库结构：`knowledge/README.md`
- 如何补充知识：`raw_knowledge/README.md`
