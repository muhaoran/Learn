# 使用指南

> 5 分钟了解如何使用 ChatBI

---

## 什么是 ChatBI

用自然语言描述数据需求，AI 自动生成符合业务口径的 Trino SQL。

```
你：[用自然语言描述要查询的指标、时间范围、分组维度]
AI：[复述需求 → 生成 SQL → 技术方案说明 → 字段说明 → 注意事项]
```

---

## 如何提需求

一个好的需求描述包含四个要素：

| 要素 | 说明 |
|------|------|
| **指标** | 要查询什么（已登记的指标名 / 别名，或可被组合推导的统计目标） |
| **时间** | 时间范围（具体日期、最近 N 天、某月份等） |
| **维度** | 是否分组、按什么分组（按已定义的维度名） |
| **筛选** | 是否限定子集（特定分群、特定状态等） |

四要素齐全时，AI 可直接生成 SQL；缺失要素或表述模糊时，AI 会主动澄清。

---

## AI 的处理步骤

AI 内部遵循与 `.cursorrules` 一致的 6 步工作流，对外可简化为 4 个可感知阶段：

1. **理解需求**（对应内部「1 需求解析」）：识别业务术语（包括黑话），提取指标、维度、时间、筛选
2. **主动澄清**（对应内部「2 需求澄清」）：遇到模糊点时，提供选项让你选择，而不是开放式提问
3. **生成 SQL**（对应内部「3 知识检索 → 4 SQL 生成 → 5 SQL 验证」）：按四层知识检索后展开 Pattern 模板，并在输出前完成自检
4. **输出说明**（对应内部「6 结果输出」）：技术方案、使用的表、字段含义、注意事项

> 完整 6 步定义见 `.cursorrules` 与 `design_philosophy/03_cursorrules_design.md`。

---

## 你会得到什么

每次输出包含：

- 格式化的 SQL（带注释，可直接执行）
- 使用的表和计算逻辑说明
- 结果字段含义
- 性能和数据质量提示

输出结构遵循 `.cursorrules` 中的「标准输出格式」。

---

## 模糊需求处理示例

如果需求缺失四要素中的关键项（例如只说「看一下数据」而没有指标/时间/维度），AI 会按以下结构澄清：

```
您的需求"[原始需求]"比较宽泛，需要更多信息：

1. [需要确认的要素 1]
   A. [选项 A]
   B. [选项 B]
   推荐：[推荐选项 + 理由]

2. [需要确认的要素 2]
   A. [选项 A]
   B. [选项 B]
```

详细的澄清规则见 `skills/02_requirement_clarification.md`。

---

## 遇到问题

| 情况 | 处理方式 |
|------|----------|
| SQL 不准确 | 告诉 AI 哪里不对，AI 重新生成；同时记录到 `feedback/corrections/` |
| AI 理解有偏差 | 纠正理解，或重新描述需求 |
| 知识库中没有该指标/概念 | AI 会说明缺少什么，请你临时提供定义后继续；同时记录到 `feedback/knowledge_gaps/` |
| 需求超出数据能力 | AI 会说明原因并提供替代方案 |
| 是新的需求类型，希望未来复用 | AI 验证 SQL 后建议沉淀到 `knowledge/`，并记录到 `feedback/new_cases/` |

---

## 反馈

发现 SQL 不准确或有改进建议，直接告诉 AI：
- AI 会按反馈类型记录到 `feedback/` 对应子目录（`corrections/` / `knowledge_gaps/` / `new_cases/`）
- 建议同时将正确的定义补充到 `knowledge/` 对应位置

---

更多信息：
- 系统架构：`README.md`
- 知识库结构：`knowledge/README.md`
- 如何补充知识：`raw_knowledge/README.md`
- AI 行为规则：`.cursorrules`
