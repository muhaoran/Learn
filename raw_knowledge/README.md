# 原始知识资料库

> 本文件夹用于存放收集到的原始知识资料，AI 会将这些原始资料转换为结构化的知识库内容。

---

## 📁 文件夹结构

```
raw_knowledge/
├── README.md                    # 本文档
├── QUICK_START.md               # 快速开始指南
│
├── mixed/                       # 混合内容的原始资料 ⭐推荐
│   ├── 综合文档.docx           # 包含多种类型内容
│   ├── 数据需求.xlsx           # 业务+数据混合
│   └── ...                     # 不确定放哪里？放这里！
│
├── business/                    # 纯业务相关原始资料（可选）
│   ├── 术语表.xlsx             # 业务术语
│   ├── 指标定义.docx           # 指标说明文档
│   └── ...
│
├── data_assets/                 # 纯数据资产相关原始资料（可选）
│   ├── 数据字典.xlsx           # 表结构和字段说明
│   ├── DDL.sql                 # 建表语句
│   └── ...
│
├── sql_examples/                # 纯 SQL 案例相关原始资料（可选）
│   ├── 常用查询.sql            # SQL 查询语句
│   ├── 需求文档.docx           # 业务需求文档
│   └── ...
│
└── processed/                   # 已处理的原始资料（存档）
    ├── 2024-01/
    ├── 2024-02/
    └── ...
```

**💡 提示**: 
- 如果你的文件**只包含一种类型**的内容，可以直接放到对应的文件夹
- 如果你的文件**包含多种类型**的内容，或者**不确定放哪里**，直接放到 `mixed/` 文件夹
- AI 会自动识别和分类内容，无需手动区分

---

## 🎯 使用流程

### 步骤 1: 放置原始资料

将收集到的原始知识资料放到对应的文件夹中：

**方式 1: 不确定放哪里？直接放到 `mixed/`** ⭐推荐

- **任何类型的文件** → `raw_knowledge/mixed/`
  - 综合性文档、数据需求、分析报告等
  - AI 会自动识别内容类型并分类处理
  - **这是最简单的方式，不需要你自己判断！**

**方式 2: 如果很明确是单一类型，可以放到对应文件夹**（可选）

- **纯业务知识** → `raw_knowledge/business/`
  - 术语表、指标定义、业务规则、计算公式等
  
- **纯数据资产** → `raw_knowledge/data_assets/`
  - 数据字典、表结构、DDL、数据血缘等
  
- **纯 SQL 案例** → `raw_knowledge/sql_examples/`
  - SQL 查询、需求文档、分析报告等

**💡 提示**: 推荐使用方式 1，简单省心！

### 步骤 2: 通知 AI 处理

在 Cursor 中告诉 AI：

```
请处理 raw_knowledge/business/术语表.xlsx，将其转换为知识库内容
```

或者：

```
我放了新的原始资料在 raw_knowledge/ 中，请帮我处理
```

### 步骤 3: AI 自动转换

AI 会：
1. ✅ 读取原始资料
2. ✅ 分析内容结构
3. ✅ 转换为标准的知识库格式
4. ✅ 生成对应的 `.md` 文件到 `knowledge/` 目录
5. ✅ 将原始资料移动到 `processed/` 存档

### 步骤 4: 审核和调整

检查生成的知识库内容，如有需要可以：
- 补充缺失的信息
- 修正错误的理解
- 调整格式和结构

---

## 📋 支持的文件格式

**简单回答：几乎所有文本格式都支持！** ✅

### 支持的格式（按优先级）

| 格式 | 支持度 | 说明 |
|------|--------|------|
| `.docx`, `.doc` | ⭐⭐⭐⭐⭐ | Word 文档，最常用，AI 可以准确识别内容 |
| `.xlsx`, `.xls` | ⭐⭐⭐⭐⭐ | Excel 表格，结构化数据，最容易处理 |
| `.txt` | ⭐⭐⭐⭐⭐ | 纯文本，最简单，AI 完全支持 |
| `.md` | ⭐⭐⭐⭐⭐ | Markdown，结构清晰，AI 完全支持 |
| `.sql` | ⭐⭐⭐⭐⭐ | SQL 文件，AI 自动识别和转换 |
| `.pdf` | ⭐⭐⭐⭐ | PDF 文档，AI 可以读取文字内容 |
| `.csv` | ⭐⭐⭐⭐ | CSV 表格，结构化数据 |
| `.json` | ⭐⭐⭐ | JSON 数据，AI 可以解析结构 |
| `.png`, `.jpg` | ⭐⭐ | 图片，需要人工辅助说明 |
| 其他文本格式 | ⭐⭐⭐ | 只要是文本，AI 都会尝试处理 |

### 文档质量要求

**关键信息：质量不高也没关系！** ✅

| 文档质量 | AI 处理能力 | 你需要做什么 |
|---------|-----------|-------------|
| ⭐⭐⭐⭐⭐ 结构清晰、内容完整 | 完全自动处理 | 什么都不用做 |
| ⭐⭐⭐⭐ 基本结构、少量缺失 | 自动处理 + 标注缺失 | 补充缺失信息即可 |
| ⭐⭐⭐ 结构混乱、信息不完整 | 尽力提取 + 提出问题 | 回答 AI 的问题 |
| ⭐⭐ 内容混杂、难以理解 | 提取部分 + 请求澄清 | 和 AI 对话澄清 |
| ⭐ 完全无结构、信息缺失严重 | 建议整理 | 简单整理后重试 |

**重要**：
- ✅ **不需要你区分**什么是术语、什么是表结构、什么是 SQL
- ✅ **不需要你标注**内容类型
- ✅ **不需要你整理**格式和结构
- ✅ AI 会**智能识别**所有内容
- ✅ AI 会**提出问题**让你确认不确定的部分

---

## 🔄 转换规则

### 业务知识转换

**原始资料** → **知识库文件**

- 术语表 → `knowledge/business/glossary.md`
- 指标定义 → `knowledge/business/metrics/{指标类型}_metrics.md`
- 维度定义 → `knowledge/business/dimensions/{维度类型}_dimensions.md`
- 计算规则 → `knowledge/business/calculation_rules/{规则名称}.md`

**转换标准**:
1. 提取所有术语和定义
2. 标准化格式（使用模板）
3. 添加分类和标签
4. 补充示例和说明
5. 标注来源和更新时间

### 数据资产转换

**原始资料** → **知识库文件**

- 数据字典 → `knowledge/data_assets/tables/{层级}/{表名}.md`
- 表关系图 → 文字描述 + `knowledge/data_assets/relationships.md`
- DDL 语句 → 提取字段信息到表文档

**转换标准**:
1. 按照表模板 (`TABLE_TEMPLATE.md`) 组织
2. 提取所有字段和类型
3. 补充字段说明和约束
4. 添加分区信息
5. 说明使用场景

### SQL 案例转换

**原始资料** → **知识库文件**

- SQL 查询 → `knowledge/sql_examples/{分类}/{案例名称}.md`
- 需求文档 → 提取需求到 SQL 案例的"业务需求"部分
- 分析报告 → 提取分析逻辑到案例说明

**转换标准**:
1. 按照案例模板 (`EXAMPLE_TEMPLATE.md`) 组织
2. 提取业务需求
3. 记录需求澄清过程
4. 补充技术方案说明
5. 添加结果解释
6. **将 SQL 转换为 Trino 语法**

---

## 📝 原始资料命名规范

### 推荐命名格式

```
{类型}_{内容描述}_{版本/日期}.{扩展名}

示例:
- 术语表_雪球业务术语_v1.0.xlsx
- 数据字典_用户表_20240101.xlsx
- SQL案例_DAU统计_最新.sql
- 指标定义_用户指标_2024Q1.docx
```

### 命名原则

1. **清晰描述**: 文件名要能看出内容
2. **添加版本**: 便于追踪更新
3. **避免特殊字符**: 不要使用空格、中文标点
4. **统一格式**: 同类文件使用相同命名格式

**💡 提示**: 不用纠结分类，文件名描述清楚内容即可，AI 会自动识别！

---

## 🎨 原始资料质量要求

### 高质量资料的特征

✅ **内容完整**
- 有明确的定义和说明
- 包含示例和场景
- 有版本和更新时间

✅ **结构清晰**
- Excel 有表头
- Word 有章节标题
- SQL 有注释

✅ **信息准确**
- 定义准确无歧义
- 数据类型正确
- SQL 语法正确

✅ **来源可靠**
- 官方文档
- 权威人员提供
- 经过验证

### 不同质量资料的处理

| 质量等级 | 特征 | AI 处理方式 |
|---------|------|-----------|
| ⭐⭐⭐⭐⭐ 优秀 | 完整、准确、结构化 | 直接转换 |
| ⭐⭐⭐⭐ 良好 | 大部分完整，少量缺失 | 转换 + 标注缺失项 |
| ⭐⭐⭐ 一般 | 信息不完整，需要补充 | 部分转换 + 提出问题 |
| ⭐⭐ 较差 | 结构混乱，信息缺失严重 | 整理建议 + 等待补充 |
| ⭐ 很差 | 无法理解或错误严重 | 拒绝处理 + 说明原因 |

---

## 💡 使用示例

### 示例 0: 处理低质量、混杂的文档 ⭐最真实

**原始文件**: `raw_knowledge/mixed/会议记录.txt`

**内容（完全没有结构，混杂在一起）**:
```
今天讨论了数据需求，DAU 是日活，要统计去重的用户数。
user_behavior 表有这些字段：user_id 用户ID，behavior_type 行为类型。
需要写个 SQL 查昨天的数据，FROM user_behavior WHERE date = 昨天。
对了，MAU 是月活，还有个留存的概念，次日留存就是第二天回来的。
表还有个 behavior_time 字段记录时间，还有个 date 分区字段。
SQL 大概是 SELECT COUNT(DISTINCT user_id) FROM ...
```

**AI 处理**（完全不需要你标注！）:

```bash
# 1. 读取纯文本内容
# 2. AI 智能分析：
#    - 识别出"DAU 是日活"是术语定义
#    - 识别出"user_behavior 表"是数据表
#    - 识别出字段信息（虽然混在文字里）
#    - 识别出 SQL 查询语句
#    - 识别出"MAU"、"次日留存"也是术语
# 3. AI 自动分类和提取：
#    - 术语：DAU、MAU、次日留存
#    - 表结构：user_behavior 表及其字段
#    - SQL：拼接出完整的查询逻辑
# 4. 生成知识库文件
# 5. 标注不确定的部分
```

**生成内容**: 自动生成多个知识库文件

**AI 反馈**（会告诉你识别结果和不确定项）:
```markdown
## 处理完成 ✅

### 原始文件
- 文件名: `会议记录.txt`
- 格式: 纯文本
- 质量: ⭐⭐⭐ 一般（内容混杂，无明显结构）

### 识别结果
✅ AI 从混杂内容中识别出：

**业务术语** (3个):
1. DAU: 日活跃用户数
2. MAU: 月活跃用户数  
3. 次日留存: 第二天回来的用户

**数据表** (1个):
- user_behavior 表
  - user_id: 用户ID
  - behavior_type: 行为类型
  - behavior_time: 记录时间
  - date: 分区字段

**SQL 需求** (1个):
- 查询昨天的用户数（去重）

### 生成文件
1. knowledge/business/glossary.md (新增 3 个术语)
2. knowledge/data_assets/tables/dwd/user_behavior.md
3. knowledge/sql_examples/user_analysis/dau_yesterday.md (已补全为完整 SQL，使用 Trino 语法)

### 需要确认的地方 ⚠️
1. user_behavior 表的完整表名确认是 dwd_user_behavior 吗？
2. "昨天"转换为了 date_add('day', -1, current_date)，对吗？
3. 次日留存的完整定义是什么？（目前只有简单描述）

### 说明
✅ 即使内容混杂、无结构，AI 也成功提取了关键信息
✅ SQL 已自动补全为完整的 Trino 语法
⚠️ 建议确认上述标注的不确定项
```

**处理反馈**:
```markdown
## 处理完成 ✅

### 原始文件
- 文件名: `数据需求文档.docx`
- 位置: `raw_knowledge/mixed/`
- 内容类型: 混合（业务术语 + 数据表 + SQL）

### 识别结果
✅ 识别到 3 种类型的内容：
1. 业务术语: 2 个
2. 数据表: 1 个
3. SQL 查询: 1 个

### 生成文件
1. `knowledge/business/glossary.md` - 新增 2 个术语（DAU, MAU）
2. `knowledge/data_assets/tables/dwd/user_behavior.md` - 新增 1 个表
3. `knowledge/sql_examples/user_analysis/dau_simple.md` - 新增 1 个 SQL 案例

### 说明
✅ 所有内容已自动分类处理
✅ 无需手动区分内容类型
```

### 示例 1: 处理术语表

**原始文件**: `raw_knowledge/business/雪球业务术语表_v1.xlsx`

**内容**:
| 术语 | 英文 | 定义 | 业务场景 |
|------|------|------|----------|
| DAU | Daily Active Users | 日活跃用户数 | 用户增长分析 |
| MAU | Monthly Active Users | 月活跃用户数 | 用户规模评估 |

**AI 处理**:

```bash
# 1. 读取 Excel
# 2. 提取所有术语
# 3. 按照 glossary.md 格式组织
# 4. 生成或更新 knowledge/business/glossary.md
# 5. 将原始文件移动到 processed/2024-01/
```

**生成内容**:
```markdown
## DAU (Daily Active Users)

**定义**: 日活跃用户数

**说明**: 统计某日有过活跃行为的去重用户数

**业务场景**: 用户增长分析

**计算规则**: 
- 统计维度: 日
- 去重维度: user_id
- 活跃定义: 参考 `is_active` 字段

**相关指标**: 
- MAU (月活跃用户数)
- WAU (周活跃用户数)

**数据来源**: `dws_user_daily.dau`
```

### 示例 2: 处理数据字典

**原始文件**: `raw_knowledge/data_assets/数据字典_用户行为表.xlsx`

**内容**:
| 字段名 | 类型 | 说明 | 示例 |
|--------|------|------|------|
| user_id | bigint | 用户ID | 123456 |
| behavior_type | varchar | 行为类型 | login |
| behavior_time | timestamp | 行为时间 | 2024-01-01 10:30:00 |

**AI 处理**:

```bash
# 1. 读取 Excel
# 2. 识别表名和字段
# 3. 按照 TABLE_TEMPLATE.md 格式组织
# 4. 生成 knowledge/data_assets/tables/dwd/dwd_user_behavior.md
# 5. 更新 relationships.md
# 6. 将原始文件移动到 processed/
```

### 示例 3: 处理 SQL 案例

**原始文件**: `raw_knowledge/sql_examples/计算DAU的SQL.sql`

**内容**:
```sql
-- 查询昨天的DAU
SELECT DATE_SUB(CURRENT_DATE(), 1) as date,
       COUNT(DISTINCT user_id) as dau
FROM user_behavior
WHERE date = DATE_SUB(CURRENT_DATE(), 1);
```

**AI 处理**:

```bash
# 1. 读取 SQL
# 2. 分析业务需求
# 3. 转换为 Trino 语法
# 4. 按照 EXAMPLE_TEMPLATE.md 格式组织
# 5. 生成 knowledge/sql_examples/user_analysis/dau_simple.md
# 6. 将原始文件移动到 processed/
```

**生成内容**: 包含需求分析、SQL语句（Trino语法）、技术方案、结果说明等完整案例。

---

## 🔍 处理后的检查清单

AI 处理完原始资料后，请检查：

- [ ] 生成的文件是否在正确的位置
- [ ] 内容是否完整（没有遗漏重要信息）
- [ ] 格式是否符合模板要求
- [ ] 示例是否清晰易懂
- [ ] SQL 是否使用 Trino 语法
- [ ] 是否有明显的理解错误
- [ ] 是否需要补充额外说明
- [ ] 原始文件是否已移动到 processed/

---

## 📊 处理记录

### 处理日志示例

| 日期 | 原始文件 | 生成文件 | 质量 | 备注 |
|------|---------|---------|------|------|
| 2024-01-01 | 术语表_v1.xlsx | glossary.md | ⭐⭐⭐⭐⭐ | 完整准确 |
| 2024-01-02 | 数据字典_用户表.xlsx | dwd_user_register.md | ⭐⭐⭐⭐ | 缺少分区说明 |
| 2024-01-03 | DAU统计.sql | dau_trend.md | ⭐⭐⭐⭐⭐ | 已转换为Trino |

---

## 🚀 批量处理

如果有大量原始资料需要处理，可以：

### 方式 1: 逐个处理

```
处理 raw_knowledge/business/术语表.xlsx
处理 raw_knowledge/data_assets/数据字典.xlsx
...
```

### 方式 2: 批量处理

```
请处理 raw_knowledge/business/ 文件夹下的所有文件
```

### 方式 3: 优先级处理

```
请按照以下优先级处理原始资料：
1. 高优先级：术语表、核心指标定义
2. 中优先级：数据字典、常用表
3. 低优先级：SQL 案例、分析报告
```

---

## ⚠️ 注意事项

### 1. 敏感信息

- ❌ 不要上传包含真实用户数据的文件
- ❌ 不要上传包含密码、密钥的文件
- ✅ 使用脱敏后的示例数据
- ✅ 使用假数据进行示例

### 2. 文件大小

- ✅ 推荐：< 10MB 的单个文件
- ⚠️ 谨慎：10-50MB 的文件（可能处理较慢）
- ❌ 避免：> 50MB 的文件（建议拆分）

### 3. 文件格式

- ✅ 优先使用结构化格式（Excel、Markdown、SQL）
- ⚠️ 图片格式需要人工辅助说明
- ❌ 避免使用加密或压缩文件

### 4. 版本管理

- ✅ 保留原始文件的版本信息
- ✅ 在 processed/ 中按日期归档
- ✅ 记录处理日志

---

## 🤖 AI 处理能力

### AI 能做什么

✅ **自动提取**
- 从 Excel 提取术语、指标、字段
- 从 SQL 提取查询逻辑和表关系
- 从文档提取业务规则和定义

✅ **格式转换**
- 将原始资料转换为标准的 Markdown 格式
- 按照模板组织内容结构
- 补充必要的章节和说明

✅ **语法转换**
- 将 MySQL/Hive SQL 转换为 Trino 语法
- 修正常见的语法错误
- 添加必要的注释

✅ **质量检查**
- 检查内容完整性
- 验证逻辑正确性
- 标注缺失项和疑问

### AI 不能做什么

❌ **理解业务语境**
- 无法完全理解业务背景
- 需要人工确认业务定义

❌ **填补缺失信息**
- 不能臆造不存在的信息
- 需要人工补充缺失的内容

❌ **验证业务正确性**
- 不能判断业务规则是否正确
- 需要业务专家审核

---

## 📞 问题反馈

如果在使用过程中遇到问题：

1. **AI 无法理解原始资料**
   - 检查文件格式是否支持
   - 尝试整理为更结构化的格式
   - 提供额外的背景说明

2. **转换结果不准确**
   - 指出具体的错误
   - 提供正确的理解
   - AI 会重新处理

3. **缺失重要信息**
   - 补充到原始资料中
   - 或在处理后手动添加
   - 更新知识库

---

## 📚 相关文档

- [业务术语模板](../knowledge/business/glossary.md)
- [指标定义模板](../knowledge/business/metrics/)
- [数据表模板](../knowledge/data_assets/tables/TABLE_TEMPLATE.md)
- [SQL 案例模板](../knowledge/sql_examples/EXAMPLE_TEMPLATE.md)
- [知识库贡献指南](../docs/knowledge_contribution_guide.md)

---

## 🎯 目标

通过使用原始知识资料库，我们希望：

1. ✅ **降低知识录入门槛** - 不需要学习复杂的格式，直接提供原始资料
2. ✅ **提高知识转换效率** - AI 自动转换，节省人工整理时间
3. ✅ **保证知识质量** - 使用标准模板，保持一致性
4. ✅ **积累知识资产** - 原始资料存档，便于追溯和更新

---

**开始使用**: 将你的第一份原始资料放到对应文件夹，然后告诉 AI "请处理 raw_knowledge/xxx"！
